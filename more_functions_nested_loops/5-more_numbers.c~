#include "main.h"

/**
 * more_numbers - prints nums
 */
void more_numbers(void)
{
	int x;

	for (x = 0; x < 15; x++)
	{
		if (x < 10)
		{
			_putchar(x + 48);
		}
		else
		{
			_putchar(x / 10 + 48);
			_putchar(x % 10 + 48);
		}
		_putchar('\n');
	}
}
